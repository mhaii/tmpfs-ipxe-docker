# Disable DNS Server
port=0

# Enable DHCP logging
log-dhcp

#dhcp-boot=pxelinux.0

# Provide network boot option called "Network Boot"
pxe-service=X86-64_EFI,"Network Boot",pxelinux

enable-tftp
tftp-root=/var/lib/tftpboot/boot

# Run as root user
user=root
